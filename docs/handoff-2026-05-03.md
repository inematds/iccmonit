# Session Handoff — iccmonit V1: refatoração do layout (slot único + overlay no left-column)

**Data:** 2026-05-03
**Versão final desta sessão:** v1.18.17
**Último commit:** `5390234` em `main`

## Where it started
Sessão começou cobrindo bugs e melhorias no `iccmonit` (TUI de monitor de sessões Claude Code). Pediu UX cleanup: chat colapsável/lateral, modal/overlay sem cobrir chat, indicadores internos, /codex pra cota OpenAI, simplificação de keys (sem duplicação), e por fim o modelo de "slot único" abaixo da Máquina (Sessões/Procs/Docker/Boot mutuamente exclusivos).

## Applied / shipped
- [confirmed] Versão atual: **`v1.18.17`** — `/home/nmaldaner/projetos/iccmonit/monitor.py:18`
- [confirmed] Slot único: `active_slot` reactive (default `"sessions"`); 1ª press de `3/4/5/6` troca slot, 2ª press abre overlay; outras teclas com overlay aberto fecham + trocam — `monitor.py` action_open_modal/_set_slot
- [confirmed] Overlay no left-column (não cobre chat): `PanelOverlay(Vertical)` substituiu `PanelModal(ModalScreen)` — `_show_overlay()` aplica `styles.display="none"` inline em todas `ALL_LEFT_SECTIONS` antes de exibir overlay
- [confirmed] Default split passou para **60/40** (era 50/50 → 70/30); subtitle do header mostra `split N/M` em tempo real ao usar `,` `.` `=`
- [confirmed] Indicadores clicáveis dentro dos painéis: `SortIndicator` (CPU/RAM em Processos), `ModeIndicator` (ativas/todas em Sessões) — `,` `s` `a` ainda funcionam como atalhos ocultos
- [confirmed] `/codex` detecta `auth_mode="chatgpt"` em `~/.codex/auth.json` (Plus/Pro): reporta `account_id` + `last_refresh`, explica que cota é por mensagens (não API)
- [confirmed] `/docker` no chat: lista + `start|stop|restart|logs`, threads em background
- [confirmed] Coleta paralela: `concurrent.futures.ThreadPoolExecutor(max_workers=5)` em `_refresh_data_worker` (system/sessions/docker/boot/procs em paralelo)
- [confirmed] Fix `MarkupError`: nova `escape_markup()` aplicada em fields dinâmicos (Docker ports IPv6 `[::]`, ProcessTable cmd/user, SessionTable project) — `monitor.py` find: `def escape_markup`
- [confirmed] `update_sessions_enriched()` e `update_processes()` adicionam colunas defensivamente se `not table.columns` (race no overlay mount)
- [confirmed] Loop de restart corrigido: deletados 128 linhas de métodos órfãos do `PanelModal` antigo que ficaram absorvidos pelo `PanelOverlay`
- [confirmed] Crash de duplicate IDs no double-press 1-6: removidos IDs fixos dos widgets internos do overlay (`QuotaBar()` sem id) — `c.remove()` é async, IDs causavam conflito
- [confirmed] Cota persistente: `_apply_refresh` só sobrescreve `_last_quota` se vier dado novo; QuotaBar mostra `cache_age` em yellow (>120s) ou red (>300s) em vez de limpar para "aguardando API..."
- [confirmed] Chat assíncrono: `_send_to_api` é `@work(thread=True)`, `_pending` impede dupla submissão; mensagem aparece imediatamente com `aguardando claude...` antes da resposta
- [confirmed] `_apply_refresh` rodando em main thread após coleta worker (sem flicker, valores antigos persistem até o swap)
- [confirmed] `find_active_transcript()` usa mtime para detectar JSONL pós-/clear (sessionId em `session.json` congela)
- [confirmed] `/help` reescrito refletindo modelo atual (sem `p/d/b`, `1-6` descrito como swap-or-overlay)
- [confirmed] Footer compacto: `q · r · 3 Proc · 4 Sess · 5 Dock · 6 Boot · , ← · . → · = 60/40`; `1/2/p/d/b/a/s/Esc` ocultos com `show=False`
- [confirmed] Push para `git@github.com:inematds/iccmonit.git` em todos os commits — último: `5390234`

## Proposed / attempted but not confirmed
- [unverified] Botão `[ X ]` clicável no header do overlay (CloseButton) — adicionado mas user não confirmou se funciona após refactor para overlay (o walk de `parent` busca `PanelOverlay` ancestor)
- [unverified] Auto-open de browser no `serve.py` (modo web) — sobe Firefox automaticamente; não testado em ambiente do user
- [?] `MarkupError` residual em test mode (não fatal) — pode reaparecer em uso real se houver brackets em outros campos não-escapados (ex.: chat history, /diag output)
- [?] Workers `_load_docker`/`_load_boot` referem `self.app` — se overlay for fechado durante coleta, `_safe_set_content` pega exceção e tenta logar no `#overlay-title` (que pode não existir mais); fallback silencioso

## Key files for next session
- `/home/nmaldaner/projetos/iccmonit/monitor.py` — TUI inteira (1700+ linhas); funções-chave: `MonitorApp.action_open_modal`, `_set_slot`, `_show_overlay`, `PanelOverlay.show_kind`, `_apply_refresh`
- `/home/nmaldaner/projetos/iccmonit/CLAUDE.md` — instruções do projeto, regra de versionamento `v1.xx.yy` (yy NÃO reseta no bump de xx; só zera quando major muda)
- `/home/nmaldaner/projetos/iccmonit/README.md` — documentação publicada (versão **v1.16.08** lá ainda — está desatualizada do v1.18.17)
- `/home/nmaldaner/projetos/iccmonit/serve.py` — modo web via `textual-serve`, bind 127.0.0.1 ou IP da LAN
- `/home/nmaldaner/projetos/iccmonit/start.sh` — entry point (`./start.sh` terminal, `./start.sh web [porta] [lan|<ip>]`)
- `/home/nmaldaner/projetos/iccmonit/config.json` — thresholds de cor, alertas, modelo do chat, `top_processes_n`
- `/home/nmaldaner/projetos/iccmonit/chat.log` — log de conversa do chat (gitignored), com erros de API + body
- Plan file: none
- Memory files touched: none

## Running state
- Background processes: none ativos (workers de refresh são curtos, terminam < 2s)
- Dev servers / ports: none (user testa interativamente com `./start.sh`)
- Open worktrees / branches: working tree em `main`, sincronizado com `origin/main` em `5390234`

## Verification — how to confirm things still work
- `cd /home/nmaldaner/projetos/iccmonit && python3 -c "import ast; ast.parse(open('monitor.py').read()); print('parse ok')"` — sem erros
- `python3 -c "import monitor; print(monitor.VERSION)"` — `v1.18.17`
- Stress test pre-existente:
  ```
  cd /home/nmaldaner/projetos/iccmonit && timeout 30 python3 -c "
  import asyncio
  from monitor import MonitorApp
  async def run():
      app = MonitorApp()
      async with app.run_test() as pilot:
          await pilot.pause()
          for k in ['3','4','5','6']:
              await pilot.press(k)
              await pilot.press(k)
              await pilot.press('escape')
              await pilot.pause(0.3)
  asyncio.run(run())"
  ```
  Esperado: sem crash; deve rodar limpo.
- `git status` em `/home/nmaldaner/projetos/iccmonit/` — `nothing to commit, up to date with origin/main`

## Deferred + open questions
- Deferred: tela de configuração (settings UI) pra editar `config.json` de dentro da TUI — user pediu mas adiamos pra próxima iteração
- Deferred: ações em containers Docker via UI clicável (Start/Stop/Restart/Logs em modal próprio) — hoje só via `/docker <nome> action` no chat
- Deferred: monitorar usuários do Claude Code em outras pastas `/home/*/.claude/sessions/` (multi-user)
- Deferred: detectar outros agentes AI rodando (Cursor, Cline, OpenCode)
- Open: README está em `v1.16.08` — atualizar para `v1.18.17` e documentar slot único + overlay (user não pediu explicitamente, mas docs ficaram defasadas)
- Open: chat colapsável/mini — user mencionou "o chat pode ficar pequeno sem uso e quando interagir ele abre" mas o redesign do overlay liberou esse espaço; se ainda quer chat colapsável, é feature separada

## Pick up here
Se o user reportar bug visual, primeiro perguntar se relançou (`q` + `./start.sh`) — várias correções dependem do processo recarregar `monitor.py`. Senão: rodar o stress test acima pra reproduzir, ou pedir o snippet do `/log` no chat para ver erro 401/MarkupError. Se for novo recurso, README está desatualizado e merece refresh antes de continuar adicionando.
